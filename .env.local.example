# Base URL fallback (used if runtime detection fails).
# Keep production domain here; runtime code will adapt for other hosts.
NEXTAUTH_URL=https://gardencommand.replit.app

# One secret shared across all envs (generate a strong value once).
NEXTAUTH_SECRET=fn98PuTFTb0=

# Google OAuth (SAME keys for all envs)
GOOGLE_CLIENT_ID=25497748713-bve5ff31g2bb1vpndbaa5721tgiahffv.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=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

# Database
DATABASE_URL="file:./dev.db"

# Authentication & Security
NEXTAUTH_SECRET=your-nextauth-secret-here
NEXTAUTH_URL=http://localhost:3000
DAILY_SYNC_WEBHOOK_SECRET=your-webhook-secret-here

# (Optional) Force HTTPS in edge proxies if needed
# TRUST_PROXY_HEADERS=true